<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.macro.mall</groupId>
    <artifactId>mall-rebuild</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-boot.version>3.5.0</spring-boot.version>
        <!--
          必须开启 -parameters：本项目只 import 了 spring-boot-dependencies BOM，没有继承
          spring-boot-starter-parent，而 @RequestParam / @PathVariable 不写名字时依赖方法参数名。
          不开这个开关，控制器会在运行时报
          "Name for argument of type [...] not specified"（spring-boot-starter-parent 默认会设置它）。
        -->
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <!--
          JUnit tag excluded from the default build. Tests tagged "integration" need a
          live MySQL/Redis; run them with -Dexcluded.groups=none after the local services
          are up (see sql/ and application-dev.yml.example).
        -->
        <excluded.groups>integration</excluded.groups>
    </properties>

    <modules>
        <module>mall-common</module>
        <module>mall-mbg</module>
        <module>mall-security</module>
        <module>mall-admin</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.4</version>
                <configuration>
                    <excludedGroups>${excluded.groups}</excludedGroups>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
