# mall 重建项目协作规范

## 项目目标

从零重建精简版 mall 电商系统。以 `document/复现文档` 为需求来源，以现有
`mall-admin-web` 与 `mall-app-web` 的调用契约为兼容边界；功能、接口路径、参数名、
响应结构和必要状态码必须兼容，内部代码、包结构和实现方式不要求与原项目相同。

本项目的交付标准是可运行、可验证、可维护的等价功能，禁止把原仓库实现整体复制过来。

## 范围与优先级

按依赖顺序完成并验收，禁止跨越尚未通过验收的前置模块：

1. 基础层：统一返回、异常、分页、Redis、日志。
2. 后台认证与动态 RBAC。
3. 商品：商品、SKU、分类、品牌、属性。
4. 商城会员。
5. 购物车和订单：算价、下单、库存、支付、超时关闭。
6. 营销：优惠券、秒杀活动和秒杀订单。

不实现 CMS、首页推荐、收藏/足迹、MongoDB、Elasticsearch 和新的前端页面，除非用户明确扩大范围。

## 技术与契约

- JDK 17、Spring Boot 3.5、Maven 多模块、MyBatis、PageHelper、MySQL 8、Redis 7、RabbitMQ、Spring Security + JWT、SpringDoc。
- 后台服务使用 8080，商城服务使用 8085；不得擅自增加 context-path。
- 成功响应码为 `200`，未登录/令牌过期响应码为 `401`；所有接口使用统一响应包装。
- 开始模块前，先阅读复现文档中对应的数据模型、接口规格、技术决策和验收项。
- 公共 API 一旦被其他模块依赖，先冻结其签名；变更必须先说明兼容性影响并征得用户同意。

## 双电脑协作与 Git 规范

- 两台电脑都从远端最新 `main` 创建短生命周期分支；分支格式：`feat/<module>-<topic>`、`fix/<topic>`、`docs/<topic>`。
- 一个分支和一个 PR 只处理一个明确主题；不要混入格式化、重构或无关文件。
- 开始工作前执行 `git fetch origin`，基于最新 `origin/main` 创建分支；推送前同步 `origin/main` 并解决冲突。
- 禁止直接推送 `main`、强推共享分支、`git reset --hard`、覆盖他人未提交改动或用整模块 checkout 回退答案代码。
- 提交应小且可回滚，使用 Conventional Commits，例如 `feat(product): add brand CRUD`；提交信息说明目的，不写空泛描述。
- 每个 PR 必须包含：需求/接口契约、涉及表和迁移、验证命令及结果、风险或未决项。通过代码审查和验收后才合并。
- 配置中的密码、令牌、数据库连接字符串不得提交。使用本地未跟踪配置和示例配置文件。

## CRUD 协作边界

智能体可以协助实现 CRUD，但在编码前必须向用户明确报告以下内容：

1. 实体/聚合、涉及的数据表和字段。
2. 接口清单：路径、方法、请求参数、响应结构、分页/排序约定。
3. 操作范围：创建、查询、更新、删除（物理删除或软删除）及批量操作。
4. 校验、唯一性、权限、审计/状态约束和关联数据影响。
5. 需要新增或修改的 Controller、Service、Mapper/Repository、DTO、SQL、测试文件。

在用户确认范围后，智能体可完成该 CRUD 的实现、测试和接口验证。对订单、库存、优惠券、支付、权限、并发和异步消息等业务逻辑，必须额外说明事务边界、幂等性、失败补偿与并发策略；不得把它们当作普通 CRUD 简化处理。

## 设计、实现与验证

- 先写出最小设计：目标、非目标、接口/数据变化、关键流程、异常路径和验收标准；复杂模块先取得用户确认。
- 只做当前需求必需的最小改动。不要为了“通用性”提前引入框架、抽象或基础设施。
- 为新增行为先编写或更新自动化测试；修复缺陷先用测试复现。
- 每次提交前至少运行与变更匹配的单元/集成测试；模块完成后执行该模块验收，并在 PR 中记录实际输出。
- 只有拿到命令输出后才能声称编译、测试或功能通过。
- 运行资源受限环境时使用 `-Xmx512m -XX:+UseSerialGC` 和 `-Ddocker.skip=true`。

## 秒杀模块约束

秒杀是项目自建亮点，按“Redis Lua 原子预扣库存 → RabbitMQ 削峰 → MySQL 条件扣减兜底 → 订单结果查询”的主线重建，而非直接复制存档。

- Redis 预扣必须同时检查库存与单用户限购，并有可验证的回滚路径。
- 消费端必须幂等；数据库扣减使用 `stock >= quantity` 条件更新。
- 明确消息发送失败、消费失败、重复消费、Redis 与数据库不一致的补偿策略。
- 状态查询键应设置合理过期时间；库存预热/重置必须有权限控制并与活动数据一致。
- 在实现完成前，用并发测试证明库存为 10 时最多创建 10 笔成功订单。

## 沟通准则

- 主动说明假设、选择与风险；需求或契约有歧义时先提问，不默默猜测。
- 解释原理和设计取舍，帮助用户掌握实现；代码可以由智能体协助完成，但每项关键设计应可由用户复述。
- 发现当前任务外的问题只报告，不顺手修改。
